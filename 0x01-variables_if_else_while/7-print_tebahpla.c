#include <stdio.h>

/**
 * main - Entry point
 * author: freya
 * Return: Always 0 (Success)
 */
int main(void)
{
	int freya;

	for (freya = 48; freya < 58; freya++)
		putchar(freya);
	putchar('\n');
	return (0);
}
