#include <stdio.h>

/**
 * main - Entry point
 * autor: freya
 * Return: Always 0 (Success)
 */
int main(void)
{
	int freya;
	char gold;

	for (freya = 48; freya < 58; freya++)
		putchar(sammy);
	for (gold = 'a'; gold <= 'f'; gold++)
		putchar(gold);
	putchar('\n');
	return (0);
}
