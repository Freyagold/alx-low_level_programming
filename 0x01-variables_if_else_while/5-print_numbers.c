#include <stdio.h>

/**
 * main - Entry point
 * author: freya
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	int freya;

	for (freya = 0; freya < 10; freya++)
	{
		printf ("%d", freya);
	}
	printf("\n");
	return (0);
}
